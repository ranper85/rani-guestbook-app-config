apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: rani-guestbook-redis-pvc
  namespace: grupp1
spec:
  storageClassName: lvms-vg1
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
